<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

<FilesMatch "(^\.env|^\.git|^composer\.|^package\.|^vite\.|^artisan$|^docker)">
    Require all denied
</FilesMatch>

Options -Indexes
